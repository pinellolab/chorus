name: simple_legnet
channels:
 - conda-forge
 - bioconda
dependencies:
 - python >=3.10
 - pytorch >=2.0

# Jupyter support (optional)
 - jupyter
 - notebook
 - ipykernel

 # science 
 - numpy>=1.21.0
 - pandas>=1.3.0
 - scipy>=1.7.0
 
 - biopython>=1.79
 
 # PyTorch ecosystem
 - pytorch>=2.0.0
 - torchvision>=0.15.0
 - torchaudio>=2.0.0
 - cudatoolkit=11.8

 - tqdm

 # visualization
 - matplotlib
 - seaborn
