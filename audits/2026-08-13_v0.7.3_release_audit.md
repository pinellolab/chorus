# v0.7.3 release audit — 2026-08-13

Second full pass of [`AUDIT_CHECKLIST.md`](AUDIT_CHECKLIST.md), this time against the
**released tree** rather than a merged preview: `main` = tag `v0.7.3` = `58ff961`, working tree
clean. The 2026-08-12 pass audited a preview of four then-open PRs; #184 changed real code
afterwards (six templates, the padding branch, ten error messages, four render paths), so the
released state had never actually been audited.

**Verdict: green, with one finding — and the finding is that the previous audit's F1 was only
half fixed.** Nothing else moved. No P0, no regression, and nothing that affects default usage.

---

## Everything that was checked, and what it measured

| § | area | result |
|---|---|---|
| 1 | install / env | `environment.yml` parses (24 deps); all 9 CLI subcommands have help; hg38 indexed; **all 9 oracle envs present** |
| 2 | HF auth | all 4 code/doc paths name `google/alphagenome-all-folds`; MCP `list_tracks` returns **331** K562 hits with no token |
| 3 | GPU / device | `linux_x86_64_cuda`; **F1 verified behaviourally** — `cuda:1` under a `4,5` mask now reaches physical 5, where before the fix it crashed |
| 4 | CDFs | all **8** oracles: exact track counts, every `effect_cdfs` row monotone, `p50 ≤ p95 ≤ p99` throughout, signed fractions to 3 dp, `perbin` present exactly where expected, **0** unsigned negative effect entries |
| 5 | Python API | 9 oracles construct; `sequence_length` matches README **and** `ORACLE_SPECS`; `ModelNotLoadedError` everywhere; invalid name names **9 of 9**; rs12740374 → `G`, rs1421085 → `T` |
| 6 | notebooks | 3 of 3 fresh executions **exit 0, 0 errors, 0 WARNING lines** (49 / 59 / 128 cells) |
| 7 | reports | 20 committed (19 with a panel): all carry the how-to-read block, **0** duplicated cell-type rows, none over 50 MiB, every config free of the hosted registry with exactly one remote reference (the 2bit) and an inline gene track |
| 8 | MCP | **24** tools, **9** oracle specs, README agrees |
| 9 | error messages | the shared reference-FASTA error names the kwarg *and* the command; the old wording is gone from all 9 oracles |
| 10 | claim drift | `5,930`, `196 kbp`, `7,612` clean in tracked files; README ↔ `ORACLE_SPECS` agree on all 9 |
| 11 | test suites | **fast 1,619 passed / 28 skipped**; **integration 145 passed / 7 skipped** |
| 12 | reproducibility | committed-example contracts 92 passed; tree clean at the tag |
| 13 | determinism | Cherimoya bitwise identical back-to-back; the cross-process report gate passes |
| 14 | edge cases | padding suite 11 passed; `chrM`/`chrY` extract; past-telomere raises `InvalidRegionError`; **2,114 bp returned 40 bp from chr1's end** (the F2 fix, holding) |
| 15 | offline | request inventory and the same-origin air-gap case both pass (in the 48 browser tests) |
| 16 | secrets | **0** `hf_…` tokens, **0** AWS keys anywhere in `examples/ audits/ docs/ chorus/` |
| 17 | dependencies | no unpinned entries |
| 18 | license | MIT, matching `setup.py`; `THIRD_PARTY.md` covers all 13 expected entries including the DOMPurify/pako and first-party-EPInformer notes added by #184 |
| 19 | release bookkeeping | 10 passed; `__version__` 0.7.3 = tag `v0.7.3`; `_HF_REVISION` pinned to `backgrounds-2026-08-12-cherimoya-fold0` |

Browser suite, run in full on the released reports: **48 passed in 2 m 04 s**.

---

## The finding

### G1 — F1 was fixed in the templates only; the **direct** load path still overwrote the mask

`ChromBPNetOracle._load_direct` and Enformer's in-process load still did the exact thing the
previous audit's F1 was about:

```python
gpu_id = self.device.split(':')[1]
os.environ['CUDA_VISIBLE_DEVICES'] = gpu_id
```

`use_environment=False` is the **`create_oracle` default**, so if anything this is the more
important half of the two.

**Why it survived a fix and a guard.** The guard I wrote for F1 enumerated
`chorus/oracles/*_source/templates/*template.py`. The checklist item that prompted F1 said
"the TF and Cherimoya **templates** assign…", and I scoped the search to what it named instead
of to the property it was about. The enumeration was over the wrong set — which is the same
mistake in miniature that F1 and #125 both exist to prevent.

**Fixed** in `fix/2026-08-13-cuda-mask-direct-path`: one shared
`chorus.core.platform.resolve_visible_ordinal`, which remaps `N` through whatever mask is set,
passes it through when there is none, and raises naming what `N` indexes when out of range.
Verified under `CUDA_VISIBLE_DEVICES=2,3`: `cuda:0` → physical 2, `cuda:1` → physical 3. The
guard now enumerates `chorus/oracles/*.py` as well, and is mutation-tested against
reintroducing the defect.

**Not a regression and not a v0.7.3 defect**: it predates every release and only fires when a
caller passes `device='cuda:N'` explicitly. Default usage never reached it.

The eight builders under `scripts/` were checked in the same sweep and are **already correct** —
they guard on `if os.environ.get("CUDA_VISIBLE_DEVICES") in (None, "")`, so an explicit mask
wins, exactly as `CLAUDE.md` records.

---

## One transient failure, and why it was not real

The first integration run reported **144 passed, 1 failed**:
`test_a_report_is_bit_exact_across_two_processes`, with
`CUDA_ERROR_OUT_OF_MEMORY` from AlphaGenome.

It was contention I caused: the notebook re-execution (§6) was running concurrently, and
`scripts/gate_end_to_end_determinism.py` spawns two AlphaGenome processes of its own with JAX
preallocating. Re-run in isolation it passed in 3 m 38 s, and a clean serial run of the whole
integration suite gave **145 passed, 0 failed**. Both results are recorded rather than only the
good one.

Worth knowing for the next audit: **do not run the notebooks and the integration suite at the
same time**, even on different `CUDA_VISIBLE_DEVICES` — that gate script sets its own mask
(`--gpu`, default `1`) and will not stay where you put it.

## Deferred, unchanged from the previous pass

* **F8** — the `SORT1_enformer` example is not reproducible run-to-run while Enformer's forward
  pass is bitwise identical in-process and across processes. Mechanism still unlocalised; in
  v0.7.3's Known limitations.
* **`tests/test_integration.py:196`** still gates on the `HF_TOKEN` environment variable and so
  skips on a host authenticated through the stored token. That MCP path stays unverified.
* A report still fetches the reference sequence, because every igv.js version requires one and
  hg38 is 3 GB. Everything else is inline; `CHORUS_IGV_SEQUENCE_URL` closes it for a site that
  serves its own copy.
